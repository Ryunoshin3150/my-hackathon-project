services:
  app:
    build:
        context: .
        dockerfile: docker/smilephoto/Dockerfile
    ports:
      - "5001:5000"
    develop:
      watch:
        - action: sync
          path: app
          target: /app
          ignore:
            - node_modules
            - .next
        - action: rebuild
          path: requirements.txt
